# Runtime dependencies for mu2edaq-downtime-logger.
# Mirrors the [project.dependencies] block in pyproject.toml.
# Install with:  pip install -r requirements.txt

PySide6>=6.6
PyYAML>=6.0
pyzmq>=25
zeep>=4.2
watchdog>=3.0
SQLAlchemy>=2.0
mu2edaq-discovery @ git+https://github.com/Mu2e/mu2edaq-discovery.git
